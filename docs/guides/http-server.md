# HTTP server

Serve HTTP on a local port. Example: [`examples/http-server.sn`](../../examples/http-server.sn).

```bash
sn run examples/http-server.sn
# in another terminal: curl http://127.0.0.1:8080/
```

Also see `examples/http-handlers.sn` and [Networking](../networking.md).
