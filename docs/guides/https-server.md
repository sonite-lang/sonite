# HTTPS server

TLS-backed server using bundled OpenSSL. Example: [`examples/https-server.sn`](../../examples/https-server.sn) with certs under `examples/certs/`.

```bash
sn run examples/https-server.sn
```

Details: [TLS and HTTPS](../tls-https.md).
