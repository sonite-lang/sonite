# HTTP client

Fetch a URL with the HTTP client. Example: [`examples/http-fetch.sn`](../../examples/http-fetch.sn).

```bash
sn run examples/http-fetch.sn
```

Background: [Networking](../networking.md).
