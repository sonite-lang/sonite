# Publish a package

1. Create a library project (`examples/packages/creator/` is a template).
2. Set `name` and `version` in `project.toml`.
3. Log in and publish:

```bash
sn login
sn publish
```

Policy and metadata: [Packages](../packages.md). Registry security: [Registry security](../security/registry.md).
